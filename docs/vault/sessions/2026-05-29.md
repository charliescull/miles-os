# Session Log — 2026-05-29

## Summary
Full rebuild of the `/finance` tab into a positions terminal (spec: `docs/vault/specs/finance-tab-rebuild-v1.md`). All 4 phases built, type-clean, and verified live against the real sheet + APIs. Net worth **$2,591.63**, weekly profit **$221.98**, 7d **+5.85%**, top-3 **AMD/GLTR/XRP**. Only the Gemini AI outlook is left on a graceful placeholder (blocked on Google billing, not code).

## Changes Made
- **DB:** `supabase/migrations/0003_finance_rebuild.sql` — additive `fin_*` tables (config, market_cache, food_weeks, food_log, networth_snapshots, outlook_cache) + RLS deny-all. (Owner ran it.)
- **Data layer:** `lib/finance/{types,constants,sheets,finnhub,calc,food,refresh,view,gemini}.ts`. Deterministic Sheets parsing (reuses the existing JWT→Drive-XLSX→exceljs plumbing, no Claude), money math, food-budget logic.
- **API:** `app/api/finance/route.ts` (GET, 12h cache), `.../refresh/route.ts` (POST manual), `.../food/route.ts`. Cron repointed `/api/finance/snapshot` → `/api/finance` in `vercel.json`.
- **UI:** `components/finance/charts.tsx` (SVG LineChart w/ interactive crosshair, Sparkline, Donut) + full rewrite of `app/finance/page.tsx` (money header w/ bank/investments dropdown + food budget, positions terminal, top-3 cards w/ hourly 7/30/60 charts, holdings rows, sector/cap donuts). On existing oklch tokens.
- **Env:** added `FINNHUB_API_KEY`, `GEMINI_API_KEY`, `TWELVEDATA_API_KEY` to `.env.local` (still need adding to Vercel for prod).

## Bugs Fixed Live
- Weekly profit was $116.78 — fuzzy label `rent` matched "Hertz Car **Rent**al", double-counting Hertz. Fixed → `total rent` → $221.98.
- All 7d P/L was 0 — Finnhub candles 403 (gated), Stooq now captcha-gated, Twelve Data 8/min too low for 14 symbols. Switched primary candle source to **Yahoo Finance** (keyless, needs `User-Agent` header). Top-3 use hourly (`range=3mo&interval=1h`, sliced 7/30/60).

## Key Findings (see spec for detail)
- `gemini-2.5-pro` is NOT free (limit 0); code defaults to `gemini-2.5-flash` (override via `GEMINI_MODEL`).
- Owner's Google account only offers **prepaid** Gemini access — no usable free tier (every project/key routed back to prepaid-$0). Key + project + restriction are now correctly configured; the 403 is gone, only the prepay balance blocks it.

## Left In Progress / Next Steps
- **Gemini outlook:** integration is built & correct; cards show news + placeholder. To enable, owner adds ~$5 prepay at ai.studio/projects → then just hit refresh (no code change). Otherwise leave as-is.
- Add the 3 finance API keys to **Vercel** env + redeploy for production.
- Optional later: chart of `fin_networth_snapshots` (portfolio value over time, already accumulating); add GOOGL avg cost (400.60) to the sheet to enable its cost-basis P/L; expand Gemini prompt with price/cost context if Pro is ever funded.
