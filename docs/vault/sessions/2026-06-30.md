# Session Log — 2026-06-30

## Summary

Shipped the entire previously-built-but-never-deployed batch (JARVIS reskin, COMMAND
center, PWA, 15-min Telegram alerts) to production, debugged a multi-layer deploy
failure, and added a new **cancel appointment** command.

The deploy had been silently failing for days. Root causes, fixed in order:
1. New code was never committed — committed & pushed.
2. **Every Vercel deploy was failing validation** because `vercel.json` declared a
   `*/5 * * * *` cron, which the Hobby plan forbids (daily-only). Removed the alert
   cron from `vercel.json`; the 5-min cadence is handled by an external cron-job.org
   job hitting `/api/cron/alerts`.
3. The Edge middleware (`proxy.ts`) didn't reliably resolve `CRON_SECRET`, so the
   alert route 307-redirected to /login. Added `/api/cron` to `PUBLIC_PATHS`; the
   route enforces its own Bearer-token auth in the Node runtime (route.ts:103).
4. A leftover **project-level `CRON_SECRET` from May 27** was overriding the shared
   env var, so runtime saw the wrong value → 401. Corrected/removed the duplicate.

Final state: `/api/cron/alerts` returns 200 with auth, 401 without. cron-job.org job
fires every 5 min. PWA assets, COMMAND center, new look all live.

## Changes Made

- `vercel.json` — removed the sub-daily `/api/cron/alerts` cron (Hobby limit).
- `proxy.ts` — `/api/cron` added to `PUBLIC_PATHS` (route self-authenticates).
- **Cancel appointment feature:**
  - `lib/router/commandParse.ts` — new `cancel_appointment` command + grammar
    (`cancel|delete|remove appointment <subject> [at <time>]`).
  - `lib/calendar/appointments.ts` — `cancelAppointment()`: matches by summary
    (optional time disambiguation), deletes the Google event via
    `deleteCalendarEvent`, marks the mirror row `canceled=true`.
  - `lib/router/routeText.ts` — dispatcher branch for `cancel_appointment`.
- Commits: `a4249b6` (initial batch), `dbc372d` (cron fix), `668287e` (middleware
  fix), `d0587e5` (cancel feature). Plus env-var corrections in Vercel.

## Config / env notes

- New env vars now live: `CRON_SECRET`, `GOOGLE_CALENDAR_ID` (shared, linked to
  miles-os, Production+Preview). The old duplicate project-level `CRON_SECRET` was
  the gotcha — project-level vars override shared ones with the same name.
- cron-job.org "Daily Schedule" job: `*/5 * * * *` → `/api/cron/alerts` with header
  `Authorization: Bearer <CRON_SECRET>`.

## Finance Overhaul v2 — full 8-phase build (branch `finance-overhaul-v2`)

Implemented `docs/vault/specs/finance-overhaul-v2.md` end-to-end, autonomously, one
commit per phase, each building green. On a feature branch (not master) for a
Vercel preview before merge.

- **Phase 1** — migration `0008_finance_overhaul.sql`: fin_holdings, fin_trades,
  fin_market_brief, fin_portfolio_score, fin_recurring, fin_credit_accounts,
  fin_credit_score, fin_spend (+ext_id/account_name), fin_config dream_target/label.
  All RLS deny-all.
- **Phase 2** — holdings moved to Supabase: `lib/finance/holdings.ts` (applyTrade w/
  ledger-derived avg cost, patchHolding, getOpenShares); refresh.ts reads fin_holdings;
  finnhub searchSymbols; /api/finance/{trade,holding,symbol-search}; TradeForm modal
  (+Trade button, inline Edit); Telegram `bought/sold` grammar; scripts/seed-holdings.ts.
- **Phase 3** — layout split: portfolio table (left, shortened 56px sparkline) + Market
  News (right); allocation donuts; empty-state.
- **Phase 4** — 7am brief: `alphavantage.ts` (budgeted ~6 calls, cached, fail-soft),
  `marketBrief.ts` (Nova narrative → fin_market_brief), `/api/cron/market-brief`
  (self-auth), MarketNews wired to live data.
- **Phase 5** — scoring: `scoring.ts` (position-weighted sentiment, Herfindahl
  diversification, risk blend, Claude risk-factors+upside); `Scores.tsx` gauges;
  `--score-div-good/bad` tokens; runs in the 7am cron.
- **Phase 6** — recurring/bills: `recurring.ts`, `billAlerts.ts` (roll-forward +
  type-aware alerts), `lib/telegram.ts`, `/api/finance/recurring`, `Recurring.tsx`
  (timeline, category rollups, cancel tracker, add form).
- **Phase 7** — credit + spend: `credit.ts` (+FICO, utilization, payment alerts),
  `spend.ts`, `simplefin.ts` (+ `scripts/simplefin-claim.ts`, explicit ext_id dedupe),
  `/api/cron/simplefin`, `/api/finance/{credit,spend}`, CreditCard/CreditForm/SpendReadout,
  Telegram `coffee 6` / `spent 12 lunch` / `$40 gas` grammar.
- **Phase 8** — DreamCar: `DreamCar.tsx` (count-up rail, progress glow) + `CaymanScene.tsx`
  (r3f turntable, glb-probe + IntersectionObserver so WebGL only runs when the model
  exists & is on-screen; SVG fallback otherwise); `.card-charge`/`.draw-on` motion,
  all gated behind prefers-reduced-motion.

### Decisions / honest notes
- Holdings now live in fin_holdings; the Google Sheet still feeds bank/liabilities/
  buying-power (not yet migrated — hybrid per spec §5.2).
- Net worth is unchanged; recurring shows a *forward* committed/mo figure and is NOT
  subtracted from net worth (spec §9.3). Realized spend goes to fin_spend only.
- SimpleFIN sync dedupes by explicit ext_id lookup rather than partial-index upsert.

### Runtime setup still required before features light up (blocked on user)
- Run migration `0008` in Supabase SQL editor.
- Env vars (Vercel, Prod+Preview): `ALPHAVANTAGE_API_KEY` (free), `SIMPLEFIN_ACCESS_URL`
  (via `npx tsx scripts/simplefin-claim.ts <token>`), optional `DREAM_TARGET`.
- cron-job.org jobs: `/api/cron/market-brief` at 07:00 America/New_York, `/api/cron/simplefin`
  at 06:30 — both with `Authorization: Bearer <CRON_SECRET>`.
- Drop a licensed 718 Cayman `.glb` at `public/models/cayman.glb` (SVG fallback until then).
- Seed positions via TradeForm / Telegram / `scripts/seed-holdings.ts`.
- Merge `finance-overhaul-v2` → master after previewing.

## Next Steps

- Test the bot live, including the new `cancel appointment Dentist at 3pm`.
- Review the `finance-overhaul-v2` Vercel preview; do the runtime setup above; merge.
- Still pending (manual, non-blocking): share Google Calendar with the service
  account ("Make changes to events"); connect WHOOP; install the PWA at `/m`.
