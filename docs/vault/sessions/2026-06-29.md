# Session — 2026-06-29

## "JARVIS reskin" — major visual overhaul (driven from the desktop via Chrome ext / Cowork)

### Design direction change (supersedes old B&W rule)
The strict "pure black + white, chroma only for signals" rule is intentionally relaxed per user request → **full Tony-Stark / JARVIS look**: electric cyan/blue as the primary HUD accent + Stark red, green/red still mean up/down signals. dont-break.md #6 (oklch only) is still honored — all CSS stays oklch; THREE.js material colors use hex (required, WebGL can't parse oklch).

### Changed
- **`app/globals.css`** — added JARVIS tokens (`--jarvis`, `--jarvis-bright`, `--jarvis-dim`, `--jarvis-deep`, `--stark-red` + Tailwind `--color-*`). Bloom halos retinted cyan (glyph core stays crisp white). New `body::before` cyan field (radial glow + faint blueprint grid, screen-blend at z-9998 so it shows over the opaque black panels). `--card-border` now faint cyan. New utils: `.glow-cyan`, `.glow-box-cyan`, `.text-jarvis`, `.border-jarvis`. New keyframes for boot: `spin-slow(/-rev)`, `arc-pulse`, `sweep-sw`, `scan-vert`, `bar-jitter`, `drift-rotate-rev` — all killed under reduced-motion.
- **Brain (`components/three/Brain.tsx`)** — now a **red cortex (`#ff3a2f`) + blue firing neurons (`#5cc6ff`)** with a faint blue synapse lattice re-added (`#2f8fff` @ 0.06). Clear steady spin (yaw 0.20+). pulseCount raised (8→8+18·liveliness). Fixes the "weird white dots" look.
- **Heart (`components/three/Heart.tsx`)** — shrunk (BASE_SCALE 0.8, was ~1.15), finer points (size 0.04), richer red `#ff3b4e`. Stage strip on `/health` reduced to `h-[11vh] min-h-[92px]`.
- **WHOOP — made the hero.** Rewrote `app/api/whoop/data/route.ts` to return recovery (+spo2, skin temp), full sleep (stages, need, efficiency, consistency, cycles, disturbances), strain + cycle HR/energy, 14-day trend arrays, and recent workouts. Rewrote `components/health/WhoopCard.tsx` into a full section: recovery ring + drivers, strain gauge, sleep summary + stage bar, 5 trend sparklines, workout list. (Uses WHOOP v2 `/recovery`,`/activity/sleep`,`/activity/workout` + v1 `/cycle`.)
- **Boot (`components/boot/BootSequence.tsx`)** — added JARVIS chrome: `ArcReactor` (concentric counter-rotating SVG rings + radar sweep) behind the cascade, `TelemetryColumn` flanks (equalizer bars + scan line + spec readout). Strokes + corner stamps + cursor retinted cyan. Timeline unchanged.
- **Home (`app/page.tsx`)** — filled the sparse left column with new **`components/dashboard/SystemCard.tsx`** (diegetic vitals: uptime, build, BlockGauges — no new data deps). Grid columns → `minmax(280px,300px)`, brain `h-[28vh]`, gridlines tinted cyan.
- **Cohesion** — Core (finance) + Constellation (CRM) organs + their page gridlines retinted cyan; TopRail border/active-tab/avatar → cyan; HudFrame brackets → cyan.

### Verify
- `npx tsc --noEmit` / `npm run build` could NOT be run this session: the sandbox's mounted copy of the repo lagged behind the saved (OneDrive cloud) files, so bash/tsc read stale/truncated copies and emitted bogus "unclosed JSX tag at EOF" errors. The actual saved files were re-read from source and confirmed complete + valid. **Run `npm run build` locally (or let Vercel build) for the definitive check before relying on it.**

### Still pending (unchanged from 06-27, user steps)
- Run migration `0006_whoop_tokens.sql` on Supabase; set Vercel env `WHOOP_CLIENT_ID/SECRET/REDIRECT_URI`; WHOOP dashboard redirect = same URL; then deploy. WHOOP card shows "Not connected" until then.

---

## COMMAND CENTER (Phase 1) — replaces CRM

New section at `/command` (nav tab CRM → COMMAND). Telegram-fed, 3 columns:
- **Left 25% — TASKS** (`components/command/TaskList.tsx`): neon-bordered boxes, one per `tasks` row (kind='task'). Check off here / phone / Telegram (all sync via `/api/todos` GET+toggle, polls 20s). Inline add. Actionable bot messages ("read 10 pages") still flow through the existing capture pipeline → kind='task' → appear here.
- **Center — SCHEDULE** (`components/command/CommandCalendar.tsx`): DAY (default) / WEEK toggle. Merges read-only Google iCal (`/api/calendar`) + the new Supabase mirror (`/api/appointments`), deduped. Week = Mon-anchored 7-col grid.
- **Right 25% — NOTES** (`components/command/NotesPad.tsx`): futuristic ruled notepad, cyan bullets, NOT checkable. Daily reset (shows selected day only) + ‹ › history through days that have notes. Inline add (today).

### Telegram command grammar (`lib/router/commandParse.ts`, wired at top of `routeText.ts`)
- `set appointment <subject> at <time> [every <freq>]` → hybrid create
- `change appointment <subject> at <old> to <new>` → re-time nearest matching appt
- `X <note>` → daily note (notes table)
Runs deterministically BEFORE the LLM intent router. Tasks/food/recipe/etc unchanged.

### Hybrid calendar (`lib/calendar/appointments.ts` + `calendarWrite.ts`)
- `createCalendarEvent` now supports `recurrence` (RRULE); added `updateCalendarEventTime` (PATCH).
- `appointments.ts`: createAppointment / createAppointmentFromText / changeAppointmentTime / listAppointments + `parseRecurrence` (freq phrase → RRULE). Writes Google AND Supabase mirror; if Google fails (not shared yet) it still mirrors so the dashboard shows the named event. `/api/appointments` expands RRULE (DAILY/WEEKLY[/BYDAY]/MONTHLY/YEARLY) over the window.

### DB — migration `0007_command_center.sql` (MUST run on Supabase)
`appointments` (google_event_id, summary, start_local/end_local text, all_day, recurrence, location, notified_at, canceled) + `notes` (note_date, text). RLS deny-all. Tasks reuse existing `tasks` table — no change.

### Verify
`npx tsc --noEmit` clean on all source (only a stale `.next/` generated file errors — regenerates on build).

### User steps to go live
1. Run `supabase/migrations/0007_command_center.sql` on Supabase.
2. For real Google Calendar writes (the "blank slot" fix): share the Google Calendar the dashboard reads with the **service-account email** (`GOOGLE_SERVICE_ACCOUNT_EMAIL`) as "Make changes to events", and set Vercel env `GOOGLE_CALENDAR_ID` to that calendar's ID. Until then, appointments still appear on the dashboard (via the Supabase mirror) but not in the phone's Google Calendar app.
3. Deploy (git push). Old `/crm` route file remains but is unlinked from nav.

## Phase 2 (NOT built yet — agreed next)
- Installable PWA (manifest + service worker) at a mobile route showing schedule + tasks only (no notes), tasks checkable.
- 15-min-before-event alert via **Telegram** message — needs a frequent scheduler (Vercel cron every ~5 min) reading `appointments` (+ iCal) and using `notified_at` to dedupe.

---

## COMMAND CENTER (Phase 2) — PWA + 15-min Telegram alerts

- **PWA install:** `public/manifest.webmanifest` (start_url `/m`, standalone, black/cyan), generated icons (`public/icon-192.png`, `icon-512.png`, `icon-maskable-512.png`, `apple-touch-icon.png`), `public/sw.js` (network-first nav cache for installability), `components/pwa/RegisterSW.tsx` registers it, root `app/layout.tsx` gains manifest/appleWebApp/themeColor/icons metadata + viewport.
- **Mobile route `app/m/page.tsx`:** compact installable view — SCHEDULE (CommandCalendar) + neon TASKS only, no notes. Tasks check off + sync.
- **Alerts:** `app/api/cron/alerts/route.ts` (GET, auth via `Bearer CRON_SECRET`) — gathers today's timed events from the appointment mirror (`expandAppointments`, refactored into `lib/calendar/appointments.ts`) + the Google iCal feed (ical.js, best-effort), and for anything starting within 15 min that isn't in `alert_log`, sends a Telegram DM (`TELEGRAM_BOT_TOKEN`/`TELEGRAM_USER_ID`) and records it. `vercel.json` adds cron `*/5 * * * *`. Migration 0007 gained `alert_log` (event_key dedupe).
- Verify: `npx tsc --noEmit` clean on all source.

### Phase 2 user steps
- Migration 0007 now also creates `alert_log` — re-run it (or run once; it's idempotent via `if not exists`).
- Set Vercel env `CRON_SECRET` (Vercel auto-sends `Authorization: Bearer <CRON_SECRET>` to crons). On Hobby plan, `*/5` crons may be throttled to daily — use an external pinger (cron-job.org) hitting `/api/cron/alerts` with that Bearer header, or upgrade to Pro.
- Install PWA: open the site on phone → `/m` → Add to Home Screen.
