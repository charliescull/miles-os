# Session — 2026-06-27

## Changed
- **Home layout:** removed `FinancePulseCard` (net worth) and `KeyBlockersCard` from `app/page.tsx`. Left column now holds only `OperatorCard`. (FINANCE nav tab + `/finance` page + top-bar NW indicator intentionally kept — user chose "home card only".)
- **Brain organ** (`components/three/Brain.tsx`): shrunk on home (`h-[44vh]` → `h-[26vh]`). Removed the synapse hairline `lineSegments` ("less lines, more organic"); cortex is now a denser (2600 → 4400) soft-sprite point cloud; pulses use the same soft sprite. New shared `components/three/sprite.ts` builds a cached radial-gradient `CanvasTexture` used as the point `map`.
- **Heart organ** (`components/three/Heart.tsx`): shrunk on health (`h-[30vh]` → `h-[22vh]`). Dropped the wireframe `lineSegments`; denser surface (NT 60→120, NV 18→40) as a pink-tinted soft-sprite point cloud. `buildHeart` no longer computes line positions.
- **Sound fully removed:** deleted `lib/sound.ts`, `components/dashboard/SoundController.tsx`, `components/dashboard/MuteToggle.tsx`. Stripped all references from `Shell.tsx`, `TopRail.tsx`, `BootSequence.tsx` (+ removed `withSound` plumbing in `BootGate.tsx`/`BootSequence.tsx`), and `HealthHeart.tsx` (`setAmbientHeartbeatBpm`). The TopRail `[ REBOOT ]` button still replays the boot visually (now silent).

## Verified
- Drove the site in-browser (Playwright). HOME, HEALTH, FINANCE, CRM all render with **0 console errors**. `npx tsc --noEmit` clean.
- Brain reads as a soft organic cloud, heart as a soft beating volume — both noticeably smaller.

## Notes / next
- Home left column is now sparse (only OperatorCard). Left intentionally per scope; revisit if the void looks too empty in daily use.

## WHOOP integration (built, NOT committed/deployed)
Adapted the pasted "vanilla HTML" WHOOP prompt to the real Next/Supabase stack. Decisions: **Supabase token storage** (not localStorage), **WHOOP as the health hero** (heart shrunk).
- `supabase/migrations/0006_whoop_tokens.sql` — `whoop_tokens` table (user_id PK, access/refresh/expires), RLS deny-all. **Must be run on Supabase.**
- `lib/whoop.ts` — server helper: authorize URL, code exchange, refresh, Supabase store, `getValidAccessToken`, v1-`/cycle` vs v2 API split. Secrets server-only.
- Routes (auth-gated): `app/api/whoop/connect` (302 → WHOOP authorize, builds URL server-side so no client-side Client ID), `…/callback` (exchange → store → redirect `/health?whoop=…`), `…/data` (normalized recovery/sleep/strain, returns `{connected:false}` when no tokens), `…/disconnect`.
- `components/health/WhoopCard.tsx` — oklch-styled card (Recovery/Sleep/Strain/HRV/RHR/Resp), connect/refresh/disconnect.
- `app/health/page.tsx` — restructured: was locked `h-[calc(100vh-40px)] overflow-hidden`; now scrollable, heart `22vh→15vh`, WHOOP hero, workout log + recipes beneath.
- Verified in-browser: 0 console errors, `tsc` clean. Card shows "Not connected" locally (table/env only in prod).
- **Manual steps remaining (user):** run migration 0006 on Supabase; set Vercel env `WHOOP_CLIENT_ID`, `WHOOP_CLIENT_SECRET`, `WHOOP_REDIRECT_URI=https://miles-os-beige.vercel.app/api/whoop/callback`; WHOOP dashboard redirect = same URL; then deploy.
- WHOOP app: "Life Tracker", dev mode (10 test users), Client ID `9f601ca8-340a-4e47-95d5-2a42137cfa22`.
