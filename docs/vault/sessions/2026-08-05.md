# Session Log — 2026-08-05

## Summary

Implemented the first mobile command-center slice for capture reliability.

## Changes Made

- Replaced the mobile schedule/tasks-only page with a focused command center containing quick capture, food totals, schedule, tasks, and sync queue state.
- Added an IndexedDB-backed capture queue with offline persistence, reconnect/interval retries, and queued/uploading/processed/failed states.
- Added an additive `capture_requests` ledger and `raw_captures.idempotency_key` migration. Mobile retries claim a key atomically before entering the existing `routeTextMessage` pipeline.
- Preserved authentication, the shared quick-capture router, embeddings/audit behavior, Shell/TopRail desktop layout, and existing oklch tokens.

## Verification

- Focused ESLint: passed for changed TypeScript/TSX files.
- `npx tsc --noEmit`: passed.
- Full lint remains blocked by 43 pre-existing errors in unrelated routes/components.
- Production build remains blocked by the environment: Next first discovers an inaccessible parent lockfile/workspace path, and the webpack fallback attempts external Google font access denied by the sandbox.

## Next Steps

- Apply migration `0010_capture_idempotency.sql` in Supabase before exercising replay protection against the deployed API.
- Manually verify `/m` offline capture, reload persistence, reconnect retry, and duplicate POST behavior.

## Follow-up Reliability Pass

- Changed the mobile queue to use IndexedDB as its source of truth, retaining a small localStorage fallback only when IndexedDB is unavailable.
- Added processing, completed, and failed states to the idempotency ledger so transient server failures can be retried with the same key.
- Wrapped the shared text router so successful requests are finalized and failed requests are reclaimable without changing the capture/classify/embed/audit flow.

## Follow-up Verification

- Focused ESLint: passed for changed TypeScript/TSX files.
- `npx tsc --noEmit`: passed.
- `git diff --check`: passed.
- `npm run build`: blocked by Next.js/Turbopack workspace-root inference attempting to read the inaccessible parent directory `C:\Users\charl`; no application compilation error was reported.
- Supabase migration still must be applied before deployed replay protection can be exercised.

## Reliability Follow-up

- Made failed/stale idempotency claims conditional on the database update result, preventing concurrent retries from both replaying a side effect.
- Added `updated_at` to the additive `capture_requests` migration and reclaim processing claims only after a 15-minute lease window, covering abandoned requests after a server crash.
- Configured the documented Next.js 16 `turbopack.root` option to scope builds to this checkout, eliminating the parent-directory permission panic.

## Closed Feedback Loop Implementation Pass

- Recovered `uploading` IndexedDB/localStorage entries as queued on reload, and merged persisted state with captures created during asynchronous startup so initialization cannot overwrite new captures.
- Added explicit retry scheduling with exponential backoff (capped at five minutes), a manual retry-now action, and an in-flight guard against client-side concurrent uploads.
- Bound idempotency keys to a SHA-256 hash of the submitted text on `/api/quick`; reused keys with different content are rejected.
- Added processing tokens and a 30-second heartbeat to the idempotency ledger. Stale processing claims are reclaimable after two minutes only when the heartbeat has stopped, preventing long-running requests from being reclaimed while active.

## Closed Feedback Loop Verification

- Focused ESLint: passed with `npx eslint` on the changed mobile/API/router files.
- `npx tsc --noEmit`: passed.
- `git diff --check`: passed.
- `npm run build`: reaches Next.js compilation but remains blocked by the sandbox denying existing Google Fonts downloads; no application compilation error was reported.
- Supabase migration `0010_capture_idempotency.sql` must be applied before deployed idempotency behavior can be exercised.

## Follow-up Verification

- Focused ESLint, `tsc --noEmit`, and `git diff --check`: passed.
- `npm run build`: reaches compilation; blocked only because the sandbox cannot fetch the four Google Fonts used by the existing layout.

## Implementer handoff

- Added durable capture verification evidence in `docs/verification.md`.
- Added a disposable post-migration SQL regression script covering atomic
  claims, retries, stale leases, hash binding, and fencing-token replacement.
- Release remains blocked on applying and executing the Supabase migration plus
  the documented concurrent/manual checks; no deployment state was guessed.

## Implementer Reliability Pass

- Added processing-token fencing checks throughout the shared text router and before final completion, so a reclaimed mobile request cannot continue as the active worker.
- Claim-loss errors now propagate instead of being swallowed by best-effort capture recording.
- Kept recent processed mobile queue entries visible for completion evidence.
- Focused ESLint, TypeScript, and `git diff --check` pass after the change.
- The additive idempotency migration remains required in the deployed Supabase project; no secrets or external state were accessed.

## Atomic Claim Correction

- Replaced the client-side read-then-update lease reclaim with the additive `claim_capture_request` Postgres function. It locks the ledger row, validates the request hash, and elects at most one active processing token for an insert, failed retry, or expired lease.
- The router now calls that RPC before entering the unchanged shared routing/classify/embed/audit pipeline. Existing token fencing checks remain in place.
- Focused ESLint, TypeScript, and `git diff --check` pass. The migration has not been applied or remotely verified in this environment.
